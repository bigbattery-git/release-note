# 프로젝트 작업 기준

## 제품 목표

이 프로젝트는 사용자가 관심 있는 GitHub Repository를 등록하면 해당 Repository의 Release 정보를 일정 주기로 확인하는 웹사이트다.

- 아직 저장되지 않은 새로운 Release를 감지한다.
- 감지한 Release 정보를 저장한다.
- OpenAI API를 사용해 Release 내용을 개발자가 읽기 쉬운 형태로 정리한다.

## 기본 기술 구성

- Next.js
- Node.js
- MariaDB

기술 구성은 사용자의 추가 요청에 따라 확장한다.

## 지켜야 할 것

1. 문서, 작업 로그, 보고 등 모든 기록은 한글로 작성한다. 단, 기술명, 고유명사, 코드 식별자, 파일 경로와 명령어는 영어 원문을 유지한다.
2. UI 디자인과 스타일링은 Tailwind CSS를 사용한다.
3. 새 라이브러리 설치가 필요하거나 설치하는 편이 유지보수에 유리하다고 판단되면, 설치 또는 관련 파일 변경 전에 이유, 대안, 영향을 사용자에게 설명하고 동의를 구한다.
4. 아키텍처 구조 변경이 필요하거나 유지보수에 유리하다고 판단되면, 변경 전에 현재 문제, 제안 구조, 영향 범위를 사용자에게 설명하고 동의를 구한다.
5. 파일 분할 또는 리팩터링이 필요하다고 판단되면 실제 변경 전에 대상, 이유, 예상 영향을 사용자에게 설명하고 동의를 구한다.
6. 변경 요청인지 불분명하면 먼저 확인한다.
7. 사용자의 최신 지시가 이 문서보다 우선한다.

## 하면 안 되는 것

1. 사용자가 질문만 한 경우에는 코드나 파일을 변경하지 않는다. 질문에는 답변, 선택지 또는 권장안만 제공한다.
2. 사용자 승인 없이 라이브러리를 설치하거나 교체하지 않는다.
3. 사용자 승인 없이 아키텍처를 변경하지 않는다.
4. 사용자 승인 없이 파일을 분할하거나 리팩터링하지 않는다.
5. 사용자 승인 없이 Tailwind CSS 이외의 방식으로 UI를 디자인하거나 스타일링하지 않는다.
6. 사용자가 요청하지 않은 기술이나 기능을 현재 요구사항으로 임의 해석하지 않는다.
